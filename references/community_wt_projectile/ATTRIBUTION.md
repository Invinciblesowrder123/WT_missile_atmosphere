# 归属与许可说明（WT-Projectile）

本目录收录自上游仓库 **`Alexmalab/projectile`**（默认分支 `main`）：

- `GameFuncs.cpp` / `GameFuncs.h` —— 炮弹阻力公式（`GetDragConstant` / `GetBallisticCoeff` / `ApplyDrag`）
- `main.cpp` —— 2D 弹道主循环

**许可状态**：上游仓库**未附带任何许可证文件**（GitHub 检测为 "no license"）。依著作权法默认「保留所有权利」。

**本项目收录方式**：仅以片段形式收录，用于**教育性/研究性参考**，展示 War Thunder 炮弹阻力的真实实现思路。著作权归原作者 `Alexmalab` 所有。

** reuse 提示**：任何超出教育/研究用途的再利用（如并入自有项目、商业使用、再分发）请先取得原作者明确许可。建议以链接引用代替整段复制：<https://github.com/Alexmalab/projectile>
